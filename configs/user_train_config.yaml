# This file is what 'make recipe' now generates automatically for you.
model_type: phase7
d_model: 256  # Smaller for a quick demo
n_layers: 4     # Smaller for a quick demo
batch_size: 2
n_seq: 256
epochs: 1
learning_rate: 0.001
num_heads: 2
local_window_size: 64
