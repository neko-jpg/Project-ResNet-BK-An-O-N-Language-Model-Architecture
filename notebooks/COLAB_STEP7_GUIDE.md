# Google ColabでStep 7ノートブックを実行する方法

## 概要

このガイドでは、Step 7（システム統合とデータ効率化）のノートブックをGoogle Colabで実行する方法を説明します。

## 手順

### 1. Google Colabでノートブックを開く

以下のいずれかの方法でノートブックを開きます：

**方法A: GitHubから直接開く**
1. [Google Colab](https://colab.research.google.com/)にアクセス
2. 「GitHub」タブを選択
3. リポジトリURL `https://github.com/neko-jpg/Project-ResNet-BK-An-O-N-Language-Model-Architecture` を入力
4. `notebooks/step7_system_integration.ipynb` を選択

**方法B: ローカルファイルをアップロード**
1. [Google Colab](https://colab.research.google.com/)にアクセス
2. 「アップロード」タブを選択
3. `notebooks/step7_system_integration.ipynb` をアップロード

### 2. ランタイムタイプを設定

GPU使用を推奨します：

1. メニューから「ランタイム」→「ランタイムのタイプを変更」
2. 「ハードウェアアクセラレータ」を「GPU」に設定
3. 「T4 GPU」を選択（無料枠で利用可能）
4. 「保存」をクリック

### 3. セットアップセルを実行

ノートブックの最初のセルを実行すると、自動的に以下が行われます：

```python
# 自動実行される内容：
# 1. Google Colab環境を検出
# 2. リポジトリをクローン
# 3. 依存関係をインストール
# 4. 作業ディレクトリを設定
```

実行結果の例：
```
Google Colab環境を検出しました

リポジトリをクローン中...
Cloning into 'Project-ResNet-BK-An-O-N-Language-Model-Architecture'...
✓ クローン完了

現在のディレクトリ: /content/Project-ResNet-BK-An-O-N-Language-Model-Architecture

依存関係をインストール中...
✓ インストール完了
```

### 4. ノートブックを順番に実行

セットアップ完了後、セルを順番に実行してください：

1. **環境確認とインポート** - PyTorchとCUDAの確認
2. **モデルとデータのロード** - ResNet-BKモデルの作成
3. **Test 1: Curriculum Learning** - カリキュラム学習のテスト
4. **Test 2: Active Learning** - アクティブラーニングのテスト
5. **Test 3: Gradient Caching** - 勾配キャッシングのテスト
6. **Test 4: Transfer Learning** - 転移学習のテスト
7. **Test 5: Integrated Training** - 統合トレーニングのテスト
8. **Summary** - 結果のまとめ

## テスト内容

### Test 1: Curriculum Learning（カリキュラム学習）
- 難易度スコアの計算
- 例の難易度順ソート
- エポックごとの段階的な難易度増加

**期待される結果**:
- 難易度分布のヒストグラム
- ソート済み難易度のプロット
- エポックごとの使用例数の増加

### Test 2: Active Learning（アクティブラーニング）
- 不確実性スコアの計算
- 最も不確実な例の選択
- 不確実性ベースのサンプリング

**期待される結果**:
- 不確実性分布のヒストグラム
- 選択された例の可視化
- Top-k選択の確認

### Test 3: Gradient Caching（勾配キャッシング）
- 類似例の勾配再利用
- キャッシュヒット率の測定
- トレーニング高速化の検証

**期待される結果**:
- キャッシュヒット率 > 0%
- 50ステップのトレーニング完了
- キャッシュ統計の表示

### Test 4: Transfer Learning（転移学習）
- 大規模コーパスでの事前学習
- ターゲットデータセットでのファインチューニング
- コスト削減の測定

**期待される結果**:
- 事前学習の完了（2エポック）
- ファインチューニングの完了（1エポック）
- コスト削減メトリクスの表示

### Test 5: Integrated Training（統合トレーニング）
- カリキュラム学習 + 勾配キャッシングの組み合わせ
- 複数の最適化の同時適用
- 総合的なパフォーマンス測定

**期待される結果**:
- 3エポックのトレーニング完了
- エポックごとのキャッシュヒット率
- 使用例数の段階的増加

## 期待される実行時間

Google Colab T4 GPUでの推定実行時間：

- セットアップ: ~2分
- Test 1 (Curriculum Learning): ~3-5分
- Test 2 (Active Learning): ~2-3分
- Test 3 (Gradient Caching): ~2-3分
- Test 4 (Transfer Learning): ~5-7分
- Test 5 (Integrated Training): ~5-7分

**合計: 約20-30分**

## 期待される結果

すべてのテストが成功すると、以下の結果が得られます：

```
=============================================================
STEP 7 SUMMARY
=============================================================

✓ All Step 7 components tested successfully!

Components verified:
  1. Curriculum Learning - Examples ordered by difficulty
  2. Active Learning - Uncertainty-based selection
  3. Gradient Caching - Cache hit rate > 0
  4. Transfer Learning - Pretrain + finetune pipeline
  5. Integrated Training - All optimizations combined

Expected Cost Reduction:
  - Curriculum learning: ~1.4× (30% fewer steps)
  - Active learning: ~2× (50% of data)
  - Gradient caching: ~1.25× (20% cache hit rate)
  - Transfer learning: ~5× (fewer epochs on target)
  - Combined: 1.4 × 2 × 1.25 × 5 = 17.5× (exceeds 10× target!)

=============================================================
STEP 7 COMPLETE ✓
=============================================================
```

## トラブルシューティング

### 問題: リポジトリのクローンに失敗

**解決策**:
```python
# 手動でクローン
!git clone https://github.com/neko-jpg/Project-ResNet-BK-An-O-N-Language-Model-Architecture.git
%cd Project-ResNet-BK-An-O-N-Language-Model-Architecture
```

### 問題: モジュールが見つからない

**解決策**:
```python
# パスを確認
import sys
sys.path.insert(0, 'src')

# 依存関係を再インストール
!pip install torch datasets transformers
```

### 問題: CUDA out of memory

**解決策**:
```python
# バッチサイズを減らす
config = {
    # ...
    'batch_size': 16,  # 32から16に変更
}

# またはランタイムを再起動
# ランタイム → ランタイムを再起動
```

### 問題: データセットのロードに失敗

**解決策**:
ノートブックは自動的にダミーデータセットにフォールバックします。
実際のWikiText-2データセットを使用する場合：

```python
!pip install datasets
from datasets import load_dataset
dataset = load_dataset('wikitext', 'wikitext-2-raw-v1')
```

## リソース

- **リポジトリ**: https://github.com/neko-jpg/Project-ResNet-BK-An-O-N-Language-Model-Architecture
- **ドキュメント**: `docs/STEP7_SYSTEM_INTEGRATION.md`
- **クイックリファレンス**: `STEP7_QUICK_REFERENCE.md`
- **完了サマリー**: `TASK_8_STEP7_COMPLETION.md`

## サポート

問題が発生した場合：

1. ランタイムを再起動してみてください
2. セルを順番に再実行してください
3. GitHubのIssuesで報告してください

## 次のステップ

Step 7のテストが完了したら：

1. **Task 9**: 包括的なベンチマーク
2. **Task 10**: 10億倍のコスト削減の検証
3. **Task 11**: 理論的分析と解釈可能性

---

**Happy Testing! 🚀**
